---
title: Overview
description: Access the files of a Dataverse dataset through an fsspec filesystem.
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';

`DataverseFS` exposes the files of a single Dataverse dataset version as an
[fsspec](https://filesystem-spec.readthedocs.io/) filesystem. Once you have an
instance, you can browse, read, write, and delete files using the standard
fsspec interface (`ls`, `info`, `open`, `cat`, `rm`, `find`, `glob`, ...) plus a
few Dataverse-specific helpers for tabular data.

It is the same machinery that powers the high-level [`Dataset`](/high-level/dataset/)
file operations — `dataset.open(...)`, `dataset.files`, `dataset.upload_file(...)`
all delegate to a `DataverseFS` under the hood — but you can also use it directly
whenever you want a filesystem-style view of a dataset.

## Why a filesystem?

Treating a dataset as a filesystem unlocks two things that are otherwise awkward
with a plain REST client:

- **Streaming, not buffering.** Reads are served by HTTP Range requests, so
  seeking or reading a slice never downloads the whole file. Writes are streamed
  to Dataverse in bounded chunks, so uploading a large file never holds it all in
  memory.
- **The fsspec ecosystem.** Importing `pyDataverse` registers a `dataverse://`
  URL protocol with fsspec. Any fsspec-aware library — pandas, Dask, Polars,
  PyArrow, Zarr — can then read a dataset file directly from a URL, with no glue
  code. This is what makes `pd.read_csv("dataverse://...")` work.

:::note
A `DataverseFS` instance is bound to **one dataset version**. Create a separate
instance for each dataset (or version) you want to work with.
:::

## Quickstart

```python
from pyDataverse.filesystem import DataverseFS

fs = DataverseFS(
    base_url="https://demo.dataverse.org",
    identifier="doi:10.5072/FK2/ABCDEF",
)

fs.ls("/")                                  # list files
with fs.open("data/notes.txt", "r") as f:   # stream a file
    print(f.read())
```

## In this section

<CardGrid>
  <LinkCard
    title="Connecting"
    href="/filesystem/connecting"
    description="Create a DataverseFS — directly, from a URL, or from a high-level Dataset."
  />
  <LinkCard
    title="Browsing & metadata"
    href="/filesystem/browsing"
    description="List, glob, and inspect files; read rich Dataverse metadata."
  />
  <LinkCard
    title="Reading files"
    href="/filesystem/reading"
    description="Stream file content in text or binary, including byte-range reads."
  />
  <LinkCard
    title="Writing files"
    href="/filesystem/writing"
    description="Create, replace, and delete files; attach metadata on upload."
  />
  <LinkCard
    title="Tabular data"
    href="/filesystem/tabular"
    description="Load ingested tabular files straight into pandas DataFrames."
  />
  <LinkCard
    title="pandas & the fsspec ecosystem"
    href="/filesystem/pandas"
    description="Read datasets by URL from pandas, Dask, Polars, and the command line."
  />
</CardGrid>
